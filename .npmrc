# Flat, hoisted node_modules (like yarn/npm) for a low-risk migration off yarn.
# Switch to pnpm's default strict symlinked layout later once phantom deps are fixed.
node-linker=hoisted
# Enforce the Node version in package.json engines (matches the old yarn behavior).
engine-strict=true
