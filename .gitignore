node_modules
coverage
.next/
.DS_Store

# local env files (keep .env.example committed)
.env*.local

# AI — .claude/skills/ IS committed (shared agent context); local settings are not
/.plans
.claude/settings.local.json

# Nx
.nx/cache
.nx/workspace-data
.nx/migrate-runs

# TypeScript
*.tsbuildinfo

# Personal TypeScript learning sandbox — keep untracked
components/TypescriptLearning/
