// Public API of the appbase assignment lib.
// Two independent screens, one per page (appbase + circles).
export { default as Appbase } from './lib';
export { default as Circles } from './lib/circles';
