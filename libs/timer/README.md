# timer

Three independent stopwatches (start / pause / stop). Self-contained, authored
in TypeScript.

```tsx
import Timer from '@my-portfolio/timer';
```

Rendered by [pages/timer.jsx](../../pages/timer.jsx); also a smoke-test target.

```bash
pnpm nx lint timer
pnpm nx typecheck timer
```
