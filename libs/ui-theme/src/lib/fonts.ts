// The portfolio site's typography tokens. Loaded from Google Fonts in
// pages/_document.tsx: Space Grotesk (display), Inter (body), JetBrains Mono
// (the utility/label face). Each has a system fallback so the shell still
// renders before the webfonts arrive.
export const FONT = {
  DISPLAY: "'Space Grotesk', system-ui, -apple-system, sans-serif",
  BODY: "'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
  MONO: "'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace",
};
